# build output
dist/
.astro/

# dependencies
node_modules/

# Soul System scaffolding — local only, never ship to the public repo
.soul/
.claude/
CLAUDE.md

# internal planning docs — local only (design spec + implementation plan).
# Ignore everything under docs/ EXCEPT the post-bundle template, which is
# a content-production contract that ships.
docs/*
!docs/post-bundle-template/

# env / editor / OS
.env
.env.production
.DS_Store
*.log
