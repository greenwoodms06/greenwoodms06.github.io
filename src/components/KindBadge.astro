---
// Small-caps label for a post's kind.
import { KIND_LABEL, type PostKind } from '../lib/content';
interface Props {
	kind: PostKind;
}
const { kind } = Astro.props;
---

<span class="font-sans text-[11px] uppercase tracking-wider text-muted">{KIND_LABEL[kind]}</span>
